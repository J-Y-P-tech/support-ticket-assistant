"""FastAPI routers for the api service (plan Task 4).

`customer` — public submit + reference-code lookup (SPEC §4.1/§4.8).
`rep` — the rep queue + ticket detail (SPEC §4.3/§4.7); draft actions land in Task 17.
"""
